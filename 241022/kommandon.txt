# Nedan söker efter ./Dockerfile
docker build -t first:latest .
docker build -t first:latest -f Dockerfile.first . # . är build context därifrån COPY osv tar saker, -f är valfri Dockerfile
docker images # lista
docker rmi first:latest # remove imagen
docker image prune -a # tar bort alla som inte är anslutna till en container
docker image prune # tar bort alla "dangling" (utan både namn och tagg)
docker exec -it elastic_shannon bash
flasks webbsida: https://palletsprojects.com/projects/flask/
docker build -t first:latest -f Dockerfile.first . # nu med dockerfile för Flask

Python för en minimal Flask-applikation:

    from flask import Flask
    app = Flask(__name__)
    x = 0

    @app.route("/")
    def hello_world():
        global x
        x = x + 1
        return f"Call {x}"

docker run -d --name test first:latest
docker exec -it test bash

flask --app /app/app.py run
Ctrl + Z
bg

docker container rm test

# 1024 på värddatorn, 5000 på containern
docker run -d --name test -p 1024:5000 first:latest
